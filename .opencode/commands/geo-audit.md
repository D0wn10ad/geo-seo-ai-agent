---
description: "Run a full GEO+SEO audit with parallel subagents on a target URL"
---

Load the `geo` skill and execute the full GEO+SEO audit workflow on `$1`. Run all 3 phases: discovery (fetch homepage, detect business type, extract key pages), parallel analysis (delegate to all 5 subagents), and synthesis (calculate composite GEO score, generate action plan).
