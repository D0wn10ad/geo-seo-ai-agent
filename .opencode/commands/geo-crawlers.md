---
description: "Check AI crawler access via robots.txt analysis on a target URL"
---

Load the `geo` skill and execute the AI crawler access check workflow on `$1`. Fetch robots.txt, parse directives for GPTBot, ClaudeBot, PerplexityBot, OAI-SearchBot, and other AI crawlers. Calculate crawler access score.
