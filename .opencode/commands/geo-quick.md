---
description: "Run a 60-second GEO visibility snapshot on a target URL"
---

Load the `geo` skill and execute the quick GEO visibility snapshot workflow on `$1`. Provide a rapid assessment of citability, crawler access, llms.txt, and brand presence with minimal depth.
