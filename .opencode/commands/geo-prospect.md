---
description: "Manage prospects through the sales pipeline"
---

Load the `geo` skill and execute the prospect management workflow. Accept subcommands via `$ARGUMENTS`: `add`, `list`, `status`, or `note`. Interact with the CRM-lite system.
