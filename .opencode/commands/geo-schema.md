---
description: "Detect, validate, and generate structured data for a target URL"
---

Load the `geo` skill and execute the schema markup workflow on `$1`. Detect existing JSON-LD, validate against Schema.org spec, check GEO-critical schemas (Organization, Person, Article, sameAs, speakable), and generate missing schema templates.
